# shipfile.yml or deploy.yml
# name: pycon-ardhi
# type: django
# python_version: 3.9

# Deployment settings
# settings:
#   static_path: staticfiles
#   wsgi_module: community.wsgi
#   requirements: requirements.txt

# Environment variables
# env:
#   DEBUG: False
#   ALLOWED_HOSTS: your-domain.deploy.tz
script: |
  python manage.py makemigrations
  python manage.py migrate
run: gunicorn community.wsgi:application --bind 0.0.0.0:3000
